export default function DailyRing({value,reference,label}:{value:number|null;reference:number|null;label:string}){const pct=value!==null&&reference!==null&&reference>0?Math.max(0,value/reference*100):null;return <div className="daily-ring" title={pct===null?'No comparison available':`${Math.round(pct)}% · ${label}`}><svg viewBox="0 0 40 40" role="img" aria-label={pct===null?'No comparison available':`${Math.round(pct)}% · ${label}`}><circle cx="20" cy="20" r="16" fill="none" stroke="currentColor" strokeOpacity=".12" strokeWidth="4"/><circle cx="20" cy="20" r="16" fill="none" stroke="currentColor" strokeWidth="4" strokeLinecap="round" pathLength="100" strokeDasharray={`${Math.min(100,pct??0)} 100`} transform="rotate(-90 20 20)"/><text x="20" y="23" textAnchor="middle" fill="currentColor" fontSize="8" fontWeight="600">{pct===null?'—':Math.round(pct)+'%'}</text></svg><small>{label}</small></div>}
