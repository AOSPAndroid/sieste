'use client';
import type {AthleteData} from './analytics';
import {useExpand} from './expansion';
export function CorosSleepWindows({windows}:{windows:Record<string,any>}){const rows=Object.entries(windows).filter(([,v])=>v.bedtime&&v.wakeTime).sort(([a],[b])=>a.localeCompare(b)).slice(-7);if(!rows.length)return null;return <section className="coros-sleep-times"><h3>Sleep timing · last 7 recorded nights</h3><table><thead><tr><th>Wake-up day</th><th>Bedtime</th><th>Wake</th><th>Score</th></tr></thead><tbody>{rows.map(([date,v])=><tr key={date}><td>{date.slice(6,8)}/{date.slice(4,6)}</td><td>{v.bedtime.slice(11)}</td><td>{v.wakeTime.slice(11)}</td><td>{v.score??'—'}</td></tr>)}</tbody></table>{rows.at(-1)&&<p className="lab-note">Latest night: deep {rows.at(-1)![1].deepPercent??'—'}% · light {rows.at(-1)![1].lightPercent??'—'}% · REM {rows.at(-1)![1].remPercent??'—'}% · awake {rows.at(-1)![1].awakeMinutes??'—'} min · naps {rows.at(-1)![1].napMinutes??'—'} min</p>}<p className="lab-note">COROS account local times. Nights belong to their wake-up day. Duration excludes awake time; scores are COROS MCP scores and can differ from Apple Health.</p></section>}
export default function CorosRecovery({data}:{data:AthleteData}){const expand=useExpand(),c=data.extra?.coros;if(data.provider!=='coros'||!c)return null;const latest=Object.entries(c.sleepWindows??{}).sort(([a],[b])=>a.localeCompare(b)).at(-1) as [string,any]|undefined,day=latest?.[0],night=latest?.[1],dayLabel=day?`${day.slice(6,8)}/${day.slice(4,6)}`:'';return <section className="coros-readings" aria-label="COROS current readings"><button onClick={()=>expand.widget('COROS sleep',<CorosSleepWindows windows={c.sleepWindows??{}}/>)}><small>Sleep score · {dayLabel}</small><strong>{night?.score??'—'}<em>/100</em></strong><span>{night?.bedtime?.slice(11)??'—'} → {night?.wakeTime?.slice(11)??'—'}</span></button><button onClick={()=>expand.widget('COROS fitness',<><strong>VO₂ max {c.fitness?.vo2max??'—'} ml/kg/min</strong><p>Current COROS fitness assessment. This is a current fitness assessment, not a per-workout estimate.</p></>)}><small>COROS VO₂ max</small><strong>{c.fitness?.vo2max??'—'}</strong><span>Current assessment</span></button><button onClick={()=>expand.widget('COROS recovery',<><strong>{c.recovery?.percent??'—'}%</strong><p>{c.recovery?.label??'No assessment supplied'}</p><p>COROS’s estimate, not a guarantee that training is safe.</p></>)}><small>COROS recovery</small><strong>{c.recovery?.percent??'—'}<em>%</em></strong><span>Current estimate</span></button></section>}
