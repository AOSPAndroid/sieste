export function calendarRange(now:Date,mode:3|7,offset:number){const start=new Date(now.getFullYear(),now.getMonth(),now.getDate());start.setDate(start.getDate()-(mode===3?2:(start.getDay()+6)%7)+offset*mode);return Array.from({length:mode},(_,i)=>{const d=new Date(start);d.setDate(d.getDate()+i);return d})}
const positive=(v:any)=>typeof v==='number'&&Number.isFinite(v)&&v>0;
export function lapPreview(laps:any[],cycling:boolean){return laps.map((lap,i)=>{const s={...lap,...lap.summary};const speed=positive(s.speed)?s.speed:positive(s.pace)?1000/s.pace:positive(s.duration)&&positive(s.distance)?s.distance/s.duration:null;return {index:i+1,value:speed,pace:speed?1000/speed:null,hr:positive(s.heartrate)?s.heartrate:null,distance:positive(s.distance)?s.distance:null,unit:cycling?'km/h':'/km'}})}
