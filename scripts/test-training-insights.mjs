import {readFileSync} from 'node:fs';
import ts from 'typescript';
import assert from 'node:assert/strict';
process.env.TZ='UTC';
function moduleUrl(name){let source=ts.transpileModule(readFileSync(new URL('../app/'+name+'.ts',import.meta.url),'utf8'),{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext}}).outputText;source=source.replace(/from ['"]\.\/([^'"]+)['"]/g,(_,dep)=>`from "${moduleUrl(dep)}"`);return 'data:text/javascript;base64,'+Buffer.from(source).toString('base64')}
const {workloadInsight,efficiencyInsight,recoveryInsight}=await import(moduleUrl('training-insights-data'));
const now=new Date('2026-09-18T12:00:00Z'),empty={historyStart:'2026-01-01',historyComplete:true,activities:[],sleep:{},hrv:{},extra:{efforts:{trainingEfforts:{}}}};
let activities=[],efforts={};for(let i=1;i<=35;i++){const d=new Date(now);d.setDate(d.getDate()-i);const key=d.toISOString().slice(0,10);activities.push({id:key,date:key+'T10:00:00Z',sportType:'running',summary:{duration:3600}});efforts[key.replaceAll('-','')]=[[i<=7?20:10]];}
let data={...empty,activities,extra:{efforts:{trainingEfforts:efforts}}};let r=workloadInsight(data,now);assert.equal(r.current.load,140);assert.equal(r.baseline,70);assert.equal(r.change,100);assert.equal(r.current.hours,7);
data.activities.push({id:'today',date:'2026-09-18T10:00:00Z',sportType:'running',summary:{duration:99999}});assert.equal(workloadInsight(data,now).current.hours,7);delete efforts['20260917'];assert.equal(workloadInsight(data,now).change,null);assert.equal(workloadInsight({...data,historyComplete:false},now).baseline,null);
activities=[];for(let i=0;i<3;i++){for(const recent of [true,false])activities.push({id:(recent?'r':'p')+i,date:`2026-${recent?'09':'08'}-${String(10+i).padStart(2,'0')}T10:00:00Z`,sportType:'running',summary:{heartrate:130+i*10,duration:3600,distance:recent?11000:10000,temperature:-5,altitude:{ascent:20}}});}
data={...empty,activities};r=efficiencyInsight(data,now,'running');assert.equal(r.pairs.length,3);assert.ok(Math.abs(r.change-10)<.001);assert.equal(new Set(r.pairs.map(p=>p.prior.id)).size,3);assert.ok(r.pairs.every(p=>p.temperatureMatched&&p.terrainMatched));assert.equal(efficiencyInsight({...data,activities:activities.slice(0,4)},now,'running').change,null);
const cold=structuredClone(data);cold.activities[0].summary.temperature=10;assert.equal(efficiencyInsight(cold,now,'running').pairs.length,2);
const cycling={...data,activities:activities.map(a=>({...a,sportType:'cycling'}))};assert.equal(efficiencyInsight(cycling,now,'cycling').change,null);cycling.activities.forEach(a=>a.summary.power=a.id.startsWith('r')?220:200);assert.ok(Math.abs(efficiencyInsight(cycling,now,'cycling').change-10)<.001);
const far=structuredClone(data);far.activities[0].summary.heartrate=190;assert.equal(efficiencyInsight(far,now,'running').change,null);
assert.equal(recoveryInsight(empty,now).tone,'neutral');assert.equal(recoveryInsight({...empty,sleep:{20260918:[5*3600]}},now).tone,'red');
const recovered=structuredClone(empty);recovered.extra.bodyvalues={bodyvalues:[]};for(let i=0;i<32;i++){const d=new Date(now);d.setDate(d.getDate()-i);const key=d.toISOString().slice(0,10);recovered.hrv[key.replaceAll('-','')]=[60];recovered.extra.bodyvalues.bodyvalues.push({timestamp:key+'T08:00:00Z',hrRestDynamic:50});}recovered.sleep={20260918:[8*3600]};assert.equal(recoveryInsight(recovered,now).tone,'green');recovered.sleep={20260918:[7.5*3600]};assert.equal(recoveryInsight(recovered,now).title,'1 signal to review');assert.equal(recoveryInsight(recovered,now).tone,'orange');
console.log('Passed: complete-day workload and missing coverage; unique matched pairs, sample minimum, HR/temperature controls, cycling power requirement; recovery baselines and sleep targets.');
