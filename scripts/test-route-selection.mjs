import ts from 'typescript';import {readFileSync} from 'node:fs';import assert from 'node:assert/strict';
const code=ts.transpileModule(readFileSync('app/route-geometry.ts','utf8'),{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext}}).outputText.replaceAll('export ','');const {routeGeometry,selectedRouteSegments}=new Function(code+';return {routeGeometry,selectedRouteSegments}')();
const geometry=routeGeometry([48,48.001,null,48.003,48.004,48.005],[2,2.001,null,2.003,2.004,2.005]);
let selected=selectedRouteSegments(geometry.segments,60,{start:1,end:5,label:'Effort 1'});assert.deepEqual(selected.map(s=>s.map(p=>p.index)),[[1],[3,4]]);assert.equal(selectedRouteSegments(geometry.segments,60,null).length,0);assert.equal(selectedRouteSegments(geometry.segments,null,{start:1,end:5,label:'a'}).length,0);assert.equal(selectedRouteSegments(geometry.segments,60,{start:2,end:3,label:'GPS gap'}).length,0);console.log('Route selection preserves source timing, GPS gaps, end boundaries and missing-data states.');
